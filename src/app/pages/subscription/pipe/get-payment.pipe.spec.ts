import { GetPaymentPipe } from './get-payment.pipe';

describe('GetPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
