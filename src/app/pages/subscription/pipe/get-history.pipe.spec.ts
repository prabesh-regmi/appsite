import { GetHistoryPipe } from './get-history.pipe';

describe('GetHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
