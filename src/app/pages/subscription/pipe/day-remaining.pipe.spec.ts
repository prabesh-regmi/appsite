import { DayRemainingPipe } from './day-remaining.pipe';

describe('DayRemainingPipe', () => {
  it('create an instance', () => {
    const pipe = new DayRemainingPipe();
    expect(pipe).toBeTruthy();
  });
});
